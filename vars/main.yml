# Global
region: "0"
common_os_support: ["CentOS", "Amazon"]
apache_certbot: True
nginx_certbot: True

php_version: "7.2"

mysql_version: "5.7"       
mysql_root_password: "123456"

mysql_databases:
 - name: wordpress
 
mysql_users:
 - name: wordpress
   host: localhost
   priv: 'wordpress.*:ALL'

init_db: 
  mysql:
    admin: root
    users: ["wordpress"]
    password: "123456"
    config_paths: 
      - /data/wwwroot/wordpress/wp-config.php
